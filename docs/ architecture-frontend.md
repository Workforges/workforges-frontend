
The UI is fully modular and multi-tenant.
Core rules:
- Do NOT hardcode forms → read from config JSON where possible
- All APIs include JWT token automatically
- Layout components shared across modules